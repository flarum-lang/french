flarum-flags:

  ##
  # UNIQUE KEYS - The following keys are used in only one location each.
  ##

  # Strings in this namespace are used by the admin interface.
  admin:

    # These strings are used in the Permissions page of the admin interface.
    permissions:
      flag_posts_label: Signaler les messages
      view_flags_label: Voir les messages signalés

    # These strings are used in the Flags Settings modal dialog.
    settings:
      guidelines_url_label: Lien vers les règles de la communauté
      title: Réglages des signalements

  # Strings in this namespace are used by the forum user interface.
  forum:

    # These strings are used by the Flag Post modal dialog.
    flag_post:
      confirmation_message: Merci d’avoir signalé ce message. Les modérateurs le vérifieront dès que possible.
      dismiss_button: => core.ref.okay
      reason_inappropriate_label: inapproprié
      reason_inappropriate_text: "Ce message est offensant, vulgaire, ou ne respecte pas les <a>règles de la communauté</a>."
      reason_off_topic_label: "hors-sujet"
      reason_off_topic_text: Ce message n’est pas pertinent à la dicussion actuelle et devrait être déplacé ailleurs.
      reason_other_label: autre
      reason_spam_label: pourriel
      reason_spam_text: Ce message est publicitaire.
      submit_button: => flarum-flags.ref.flag_post
      title: => flarum-flags.ref.flag_post

    # These strings are used by the Flagged Posts dropdown, a.k.a. "the flag".
    flagged_posts:
      empty_text: Aucun signalement
      title: => flarum-flags.ref.flagged_posts
      tooltip: => flarum-flags.ref.flagged_posts

    # These strings are used by the frame displayed around flagged posts.
    post:
      dismiss_flag_tooltip: Annuler le signalement
      flagged_by_text: "{username} a signalé ça"
      flagged_by_with_reason_text: "{username} a signalé ça comme {reason}"

    # These strings are used by the post control buttons.
    post_controls:
      flag_button: Signaler

  ##
  # REUSED STRINGS - These keys should not be used directly in code!
  ##

  # Strings in this namespace are referenced by two or more unique keys.
  ref:
    flag_post: Signaler le message
    flagged_posts: Messages signalés
